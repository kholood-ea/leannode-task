import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: "space-around", marginHorizontal: 20 },
  title: { fontSize: 20, fontWeight: "bold" },
});
